/**
 * Spacing values for consistent layout throughout the app
 */

const spacing = {
  // Base scale
  xxs: 2,     // Tiny spacing
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  
  // Specific uses
  screenPadding: 16,
  cardPadding: 16,
  inputPadding: 12,
  itemSpacing: 16,
  sectionSpacing: 32,
};

export default spacing; 